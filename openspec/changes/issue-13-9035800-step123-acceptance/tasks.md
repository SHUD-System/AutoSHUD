## 1. Fixture Data

- [x] 1.1 Add repository-contained 9035800 soil fixture data clipped from HWSD-style input to the 9035800 buffered extent, with the attributes required by `Sub_iSoil_0.2.R` / `Fun.Soil_Geol.R`.
- [x] 1.2 Add repository-contained 9035800 landuse fixture data clipped from USGS LCI-style input to the 9035800 buffered extent, plus any small color/attribute files needed by existing scripts.
- [x] 1.3 Verify and document that committed soil/landuse fixtures are clipped small files and not global source rasters.

## 2. Config And Harness

- [x] 2.1 Add a 9035800 self-contained acceptance config/template using only repository-relative input paths for GIS/DEM/soil/landuse/meteo.
- [x] 2.2 Add an Rscript acceptance harness that creates a temporary run directory, writes or patches the config `dir.out` and `dout.forc`, stages `Example/9035800/forcing/54904.csv` and `54905.csv` into the temporary forcing output directory, and runs Step1, Step2, and Step3 in order.
- [x] 2.3 Ensure the harness uses ordinary AutoSHUD step scripts instead of duplicating Step1-3 logic, except for temporary config creation, forcing staging, and output assertions.
- [x] 2.4 Ensure the harness fails clearly if any source fixture input is missing, if forcing IDs do not match CSV basenames, or if outputs would be written outside the temporary run directory.
- [x] 2.5 Ensure the harness runs Step scripts from harness-owned temporary work directories and proves repository-root `Rplots.pdf` is not deleted or modified.

## 3. Acceptance Assertions

- [x] 3.1 Assert Step1 outputs exist: `DataPre/pcs/wbd.shp`, `DataPre/pcs/stm.shp`, `DataPre/pcs/dem.tif`, and GCS watershed/buffer outputs.
- [x] 3.2 Assert Step2 outputs exist and are readable: clipped soil/geol rasters or shapefiles, `SOIL.csv`, `GEOL.csv`, `landuse.tif`, and local forcing output copies.
- [x] 3.3 Assert Step3 outputs exist and are readable: SHUD model input directory, key `.mesh`, `.riv`, `.att`, `.forc`/forcing metadata files, and GIS sidecars for domain/river/segment outputs where generated by the scripts.
- [x] 3.4 Assert basic schema/quality: non-empty stream/features, finite raster values in clipped fixtures/outputs where sampled, non-empty forcing metadata, and no unsafe forcing IDs.
- [x] 3.5 Assert source preservation: `Example/9035800` source files remain byte-for-byte unchanged or at least unchanged by git status after acceptance.

## 4. Documentation And Regression

- [x] 4.1 Document the acceptance command and fixture boundary in README/docs/release notes or a dedicated test README.
- [x] 4.2 Run `Rscript tests/test-9035800-step123-acceptance.R` successfully.
- [x] 4.3 Run `Rscript tests/test-step-forcing-hardening.R` successfully.
- [x] 4.4 Run `Rscript tests/test-era5-forcing.R` successfully.
- [x] 4.5 Inspect git diff to confirm no generated model outputs, validation reports, global soil/landuse rasters, or source fixture mutations are committed.

## 5. Review-Added Guard Evidence

- [x] 5.1 Add fixture validation that asserts soil and landuse rasters are under `testdata/9035800/geodata/...`, have non-global extents, have no more than 5,000,000 cells, and each raster file is under 50 MB.
- [x] 5.2 Add forcing-window assertions for staged `54904.csv` and `54905.csv`: after Step3, output copies remain parseable and cover the configured 2001-2005 acceptance window, while source CSVs remain unchanged.
- [x] 5.3 Inspect dependency metadata and git diff to confirm no new heavy runtime dependency or dependency metadata churn was introduced.

## 6. rSHUD Compatibility Evidence

- [x] 6.1 Add focused checks that `GetReady.R` preserves legacy `sp.RiverSeg` fallback and modern sf-preserving `shud.rivseg` call shapes.
- [x] 6.2 Add focused checks that `ReadProject.R` fallback Albers CRS derivation supports both sf-capable and legacy Spatial-only `rSHUD::crs.Albers()` behavior.
- [x] 6.3 Verify existing explicit `fsp.crs` files remain authoritative and are not replaced by fallback CRS derivation.
